#include "laplacianthing.h"
LaplacianThing::LaplacianThing()
{

}

//set<int> LaplacianThing::FriendlyNeighbourhoodSpiderman(int i, int j)
//{
//    set<int> intersected; //this set will hold the intersected vertices
//    set_intersection(P2P.at(i).begin(), P2P.at(i).end(), P2P.at(j).begin(), P2P.at(j).end(),
//                     std::inserter(intersected, intersected.begin()));
//    return intersected;
//}

//set<int> LaplacianThing::getFriends(int i){
//    set<int> neighI(P2P[i]);
//    set<int> intersected;
//    for(set<int>::iterator it(neighI.begin()); it!=neighI.end(); ++it){
//            set<int> dum = FriendlyNeighbourhoodSpiderman(i, *it);
//            intersected.insert(dum.begin(), dum.end());
//    }
//    return intersected;
//}


